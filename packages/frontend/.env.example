# Address of the deployed GameContract on Base Mainnet (chain ID 8453).
# Leave empty until the contract has actually been deployed and verified.
NEXT_PUBLIC_GAME_CONTRACT_ADDRESS=

# Optional RPC override. If omitted, wagmi uses its Base public transport.
# NEXT_PUBLIC_BASE_RPC_URL=

